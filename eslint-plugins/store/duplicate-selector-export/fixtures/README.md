# duplicate-selector-export fixtures

This aggregate-rule plugin currently uses inline verifier cases because its diagnostics depend on validation-scope aggregation rather than a single reusable fixture pair.
