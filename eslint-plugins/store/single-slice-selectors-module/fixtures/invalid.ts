/*
Invalid layout for themis/single-slice-selectors-module:

src/slices/todos/todos-slice.ts
src/slices/todos/archive-slice.ts
src/slices/todos/todos-selectors.ts
src/slices/todos/archive-selectors.ts

Split archive into src/slices/archive/archive-slice.ts and
src/slices/archive/archive-selectors.ts so each slice directory has one
*-slice module and one *-selectors module.
*/
export {};